<!DOCTYPE html>
<html>
  <head>
    <title>Ads - Nuvem</title>
    <style>
      form {
        max-width: 600px;
        padding: 2rem;
        border: 1px solid #ccc;
        border-radius: 10px;
      }
      
      label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: bold;
      }
      
      input[type="text"], input[type="number"], select {
        display: block;
        width: 100%;
        padding: 0.5rem;
        margin-bottom: 1rem;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 1rem;
      }
      
      button[type="submit"] {
        display: block;
        margin-top: 1rem;
        padding: 0.5rem;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem;
        transition: background-color 0.2s ease-in-out;
      }
      
      button[type="submit"]:hover {
        background-color: #0062cc;
      }
    </style>
  </head>
  <body>
    <h1>Atualizar ID: <%= objeto.objectId %></h1>
    <form method="post" action="/<%= nomeCrudPlural %>/update/<%= objeto.objectId %>" enctype="application/x-www-form-urlencoded">
      <label for="data">Data:</label>
      <input type="text" id="data" name="data" value="<%= objeto.data %>" required >

      <label for="vendedor">Vendedor:</label>
      <input type="text" id="vendedor" name="vendedor" min="0" step="1" value="<%= objeto.vendedor %>" required>

      <label for="carro">Carro:</label>
      <input type="text" id="carro" name="carro" value="<%= objeto.carro %>" required>

      <button type="submit">Atualizar</button>
    </form>
  </body>
</html>